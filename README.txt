Prerequisites:

 * pytz needs to be installed
 ```
 sudo apt-get install python-pip
 sudo pip install pytz
 ```

 * Provide graphics for each route that will be displayed (e.g. stadtbus3.png for the #3 bus route)
 
Installation:

    None

Usage:

    Start info-beamer for this directory.
    Then start ./service to fetch updates
